var require = {
    deps: ['app-global'],
    locale: 'en',
    baseUrl: '/Scripts',
    paths: {
        // 3rd party libs
        // jquery
        "jquery": "jquery-1.9.1"
    },
    shim: {
        //"mockjax": { deps: ["jquery"] },
        //"underscore": { deps: [], exports: '_' }
    }
};