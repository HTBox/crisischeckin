require(['jquery', 'amplify'], function($, amplify) {
    var controller = function() {
        var init = function() {
            
        };

        return {
            init: init
        };
    }();

    controller.init();
});